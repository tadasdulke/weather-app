export enum Errors {
  SomethingWentWrong = 'Something went wrong!',
  NoLocationProvider = 'Location provider was not initialized!',
  CollectionFull = 'Collection is full',
}

export enum LocationSearchConfig {
  MaxAmountOfLocationsToDisplay = 30,
}
